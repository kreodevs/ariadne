# DataTable

Wrapper sobre **TanStack Table** + componentes `Table` de Shadcn: ordenación por columna y filtro global (`includesString`).

Usado en listados densos (p. ej. repositorios). Importar `DataTable` desde `@/components/data-table/DataTable`.
